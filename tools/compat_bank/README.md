# Compat bank (release-time — Phase 26.9996 / UPD-06)

Throwaway demos only.

These dated sample trees exist so `tools/compat_check.py` can drive both
directions of the only-ever-add promise (map #144 / D-04 / D-05) against
known, disposable shapes. They are never her live library.

Rules:

- Never point samples at `~/StudyRoom` or any real library path.
- Never import or copy a live key (`keys.json`, Anthropic credentials, etc.).
- Never copy personal notes, photos, or vault content into this bank.
- Samples are synthetic / hand-built for the check; treat them as disposable.
- The bank only grows. Deleting a past date's sample is a visible intentional
  act — do it only with a deliberate check edit and her release note.

`freeze_manifest.json` lists keys/files new writers may emit; it grows only.
Suite-local fence copy also lives at `tests/fixtures/compat_bank/README.md`.
